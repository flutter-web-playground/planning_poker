# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on push

on:
  push:
    branches: [ main ]

env:
  FLUTTER_KEYS_PATH: "./keys.json"
  JAVA_VERSION: "18.x"
    
jobs:
 build_and_deploy:
    name: Build and Deploy (Firebase)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v1
        with:
          java-version: ${{env.JAVA_VERSION}}
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          channel:  'stable'
      - run: |
          echo { >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"APIKEY\":\"\${{ secrets.APIKEY }}\", >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"APPID\":\"\${{ secrets.APPID }}\", >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"MESSAGINGSENDERID\":\"\${{ secrets.MESSAGINGSENDERID }}\", >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"PROJECTID\":\"\${{ secrets.PROJECTID }}\", >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"AUTHDOMAIN\":\"\${{ secrets.AUTHDOMAIN }}\", >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"DATABASEURL\":\"\${{ secrets.DATABASEURL }}\", >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"STORAGEBUCKET\":\"\${{ secrets.STORAGEBUCKET }}\", >> ${{ env.FLUTTER_KEYS_PATH }}
          echo \"MEASUREMENTID\":\"\${{ secrets.MEASUREMENTID }}\" >> ${{ env.FLUTTER_KEYS_PATH }}          
          echo } >> ${{ env.FLUTTER_KEYS_PATH }}
          cat ${{ env.FLUTTER_KEYS_PATH }}
      - run: flutter create --platforms web .
      - run: flutter clean
      - run: flutter pub get
      - run: flutter build web --release --dart-define-from-file=${{ env.FLUTTER_KEYS_PATH }} --build-number=${GITHUB_RUN_NUMBER}
      - run: flutter build web --release -o build/web-github --base-href='/planning_poker/' --dart-define-from-file=${{ env.FLUTTER_KEYS_PATH }} --build-number=${GITHUB_RUN_NUMBER}
      - run: |
         cd build/web-github
         git init
         git config --global user.email p.h.ferreirah@gmail.com
         git config --global user.name phferreira
         git status
         git remote add origin https://${{secrets.commit_secret}}@github.com/flutter-web-playground/planning_poker.git
         git checkout -b gh-pages
         git add --all
         git commit -m "Build and Deploy from main"
         git push origin gh-pages -f
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PLANNING_POKER_6AAA3 }}'
          channelId: live
          projectId: planning-poker-6aaa3
